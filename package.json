{
  "name": "duktape-wasm",
  "version": "0.1.0",
  "description": "bindings to duktape in the browser",
  "main": "dist/duktape-wasm.min.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "start": "budo tests/sandbox.js --live",
    "build": "run-s build:*",
    "build:1": "emcc --memory-init-file 0 src/main.c lib/**/*.c -o build/duktape.js -s EXTRA_EXPORTED_RUNTIME_METHODS='[\"cwrap\"]' -s MODULARIZE=1 -s 'EXPORT_NAME=\"loadWASM\"' -s SINGLE_FILE=1",
    "build:2": "browserify -o dist/duktape-wasm.min.js src -p tinyify",
    "test": "tape tests"
  },
  "keywords": [
    "duktape"
  ],
  "author": "kareniel",
  "license": "MIT",
  "devDependencies": {
    "budo": "^11.6.0",
    "npm-run-all": "^4.1.5",
    "tape": "^4.10.1",
    "tinyify": "^2.5.0"
  }
}
